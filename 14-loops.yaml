- name: loops demo
  hosts: web
  become: yes
  tasks: 
  - name: loops
    ansible.builtin.package:
     name: "{{ item }}"
     state: present
    loop:
    - mysql
    - httpd
    - postfix
    - nginx
     